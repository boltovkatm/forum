const {Pool}=require('pg');const crypto=require('crypto');
const pool=new Pool({connectionString:process.env.DATABASE_URL,ssl:process.env.NODE_ENV==='production'?{rejectUnauthorized:false}:undefined});
const send=(res,status,data)=>res.status(status).json(data);const body=req=>typeof req.body==='string'?JSON.parse(req.body||'{}'):(req.body||{});const validNick=nick=>typeof nick==='string'&&/^[A-Za-z0-9_]{3,16}$/.test(nick);
const hash=(value,salt=crypto.randomBytes(16).toString('hex'))=>new Promise((resolve,reject)=>crypto.scrypt(value,salt,64,(e,k)=>e?reject(e):resolve(`${salt}:${k.toString('hex')}`)));const compare=async(value,stored)=>{const [salt,expected]=stored.split(':');const actual=await hash(value,salt);return crypto.timingSafeEqual(Buffer.from(actual.split(':')[1],'hex'),Buffer.from(expected,'hex'));};
async function setup(){await pool.query(`CREATE TABLE IF NOT EXISTS accounts(id SERIAL PRIMARY KEY,nickname VARCHAR(16) UNIQUE NOT NULL,password_hash TEXT NOT NULL,created_at TIMESTAMPTZ DEFAULT now());CREATE TABLE IF NOT EXISTS registration_codes(id TEXT PRIMARY KEY,nickname VARCHAR(16) NOT NULL,code TEXT NOT NULL,expires_at TIMESTAMPTZ NOT NULL,delivered BOOLEAN DEFAULT false);CREATE TABLE IF NOT EXISTS applications(id SERIAL PRIMARY KEY,role TEXT NOT NULL,payload JSONB NOT NULL,created_at TIMESTAMPTZ DEFAULT now());CREATE TABLE IF NOT EXISTS player_profiles(nickname VARCHAR(16) PRIMARY KEY,privilege TEXT NOT NULL DEFAULT 'Игрок',donate_balance TEXT NOT NULL DEFAULT '0',balance TEXT NOT NULL DEFAULT '0',playtime TEXT NOT NULL DEFAULT '—',updated_at TIMESTAMPTZ DEFAULT now());CREATE TABLE IF NOT EXISTS sessions(token_hash TEXT PRIMARY KEY,nickname VARCHAR(16) NOT NULL,expires_at TIMESTAMPTZ NOT NULL)`);}
const cookies=req=>Object.fromEntries(String(req.headers.cookie||'').split(';').map(x=>x.trim().split('=').map(decodeURIComponent)).filter(x=>x.length===2));const session=async req=>{const token=cookies(req).boltovka_session;if(!token)return null;const r=await pool.query('SELECT nickname FROM sessions WHERE token_hash=$1 AND expires_at>now()',[crypto.createHash('sha256').update(token).digest('hex')]);return r.rows[0]?.nickname||null;};const createSession=async nickname=>{const token=crypto.randomBytes(32).toString('hex'),tokenHash=crypto.createHash('sha256').update(token).digest('hex');await pool.query("INSERT INTO sessions(token_hash,nickname,expires_at) VALUES($1,$2,now()+interval '30 days')",[tokenHash,nickname]);return token;};
const pluginAuth=req=>{const received=String(req.headers['x-boltovka-plugin-key']||''),expected=String(process.env.BOLTOVKA_PLUGIN_KEY||'');return received.length>0&&received.length===expected.length&&crypto.timingSafeEqual(Buffer.from(received),Buffer.from(expected));};module.exports={pool,send,body,validNick,hash,compare,setup,pluginAuth,crypto,session,createSession,cookies};
