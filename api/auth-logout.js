const {pool,send,cookies,crypto}=require('./lib');module.exports=async(req,res)=>{if(req.method!=='POST')return send(res,405,{error:'Method not allowed'});const token=cookies(req).boltovka_session;if(token)await pool.query('DELETE FROM sessions WHERE token_hash=$1',[crypto.createHash('sha256').update(token).digest('hex')]);res.setHeader('Set-Cookie','boltovka_session=; Path=/; HttpOnly; SameSite=Lax; Max-Age=0; Secure');send(res,200,{ok:true});};
